# Node.js Provisioner API

**Author**: Jared (172.31.43.104)

## Status
Code pending push from Jared's instance.

## Endpoints (from Jared's docs)
- `POST /clusters` - Spawn cluster
- `GET /clusters/:id` - Cluster status
- `GET /agents` - List all agents
- `DELETE /agents/:id` - Terminate agent
- `DELETE /clusters/:id` - Terminate cluster
- `GET /health` - Health check

## To integrate
See INTEGRATION.md (coming from Jared)
